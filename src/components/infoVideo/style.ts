import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginRight: 50,
  },
  text: { elevation: 2, lineHeight: 16 },
  bodyPost: {
    marginTop: 3,
    paddingStart: 10,
  },
});

export const stylesBtnVerMas = StyleSheet.create({
  container: {
    flexDirection: 'row',
    padding: 2,
    alignItems: 'center',
    alignSelf: 'baseline',
    marginStart: '55%',

  },
});
